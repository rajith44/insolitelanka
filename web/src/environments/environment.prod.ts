export const environment = {
  production: true,
  apiUrl: 'https://api.insolitelanka.com/api', // Set to your API base URL in production, e.g. 'https://yoursite.com/api'
  recaptchaSiteKey: '' // Optional: Google reCAPTCHA v2 site key
};
